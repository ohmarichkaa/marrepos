#pragma once
#include <iostream>
#include <fstream>
#include "Graph.h"

using namespace std;

void PrintMatrix(int [][SIZE], int, int);

void FwriteMatrix(string, int [][SIZE], int, int);

void FreadMatrix(string, int [][SIZE], int, int);